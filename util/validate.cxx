
#include "FastNetTool/neuralnetwork/NeuralNetwork.h"
#include "FastNetTool/neuralnetwork/Backpropagation.h"
#include "FastNetTool/neuralnetwork/RProp.h"
#include <iostream>
#include <vector>

using namespace std;
using namespace msg;
using namespace FastNet;

int main(int argc, const char* argv[]){

  /*
  INeuralNetwork inet(100,3,1,"tansig",true);
  
  
  //NeuralNetwork *net = new NeuralNetwork(inet, DEBUG);
  RProp *net = new RProp(inet, DEBUG);
  net->showInfo();
  delete net;
  */
  return 1;
}
